apiVersion: v1
kind: Service
metadata:
  name: complaint-analyzer-service
  namespace: "2401070"
spec:
  selector:
    app: complaint-analyzer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
